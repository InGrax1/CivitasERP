<Page x:Class="CivitasERP.Views.ConfigurationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CivitasERP.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConfigurationPage"
      Background="{StaticResource BackgroudColor}">

    <Grid>
        <Border Background="{StaticResource BackgroudColor}"
                BorderBrush="LightGray"
                BorderThickness="2"
                CornerRadius="10"
                Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Configuracion"
                           Grid.Row="0"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource titleColorA}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Especificaciones del programa"
                           Grid.Row="1"
                           FontSize="15"
                           FontWeight="Normal"
                           Margin="5"
                           Foreground="{StaticResource titleColor3}"
                           HorizontalAlignment="Center"/>
                <Grid Grid.Row="2" Margin="10" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <Image Source="/Images/AdminPanel.png" Grid.Column="0" Height="20" Width="20"/>
                        <TextBlock Text="Version"
                                   Grid.Column="1"
                                   FontWeight="Normal"
                                   Width="100"
                                   HorizontalAlignment="Left"/>
                        <TextBlock Text="CivitasERP v1.1.3" FontWeight="SemiBold" Grid.Column="1"/>
                    </StackPanel>
                    
                    
                </Grid>
            </Grid>
        </Border>

    </Grid>
</Page>
